import React from 'react';
import Event from './Event/Event';
import Aux from '../../hoc/Aux';

const Feed = (props) => {

    return (

        <Aux>
            <Event/>
            <Event/>
            <Event/>
        </Aux>

    );
};

export default Feed;
